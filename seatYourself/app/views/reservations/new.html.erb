<h3>Make Your Reservation</h3>
<p> Make your reservation for <%= @restaurant.name %></p>
<br><p>What day would you like to come in?</p>

<%= form_for [@restaurant, @reservation] do |f|%>

	<p>
		<%= f.label :reservation_date %>
		<%= f.datetime_select :reservation_date %>
	</p>

	<p>
		<%#= f.label :time %>
		<%#= f.time_select :time %>
	</p>

	<p> 
		<%= f.label "Seats"%> <%= f.text_field :partySize %> 
	</p>
		
	<p><%= f.submit "Submit"%></p>

<% end %>